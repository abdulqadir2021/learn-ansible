- name: Demo on variables
  hosts: localhost
  connection: local
  vars:
    URL: vars.google.com
  tasks:
    - name: print URL
      ansible.builtin.debug:
        msg: URL - {{ URL }}


# Here in the above msg line, we have th vle